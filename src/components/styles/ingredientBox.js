import styled from "styled-components";

const IngredientBox = styled.label`
  display: inline-block;
  width: 50%;
  padding: 2px;
  font-size: 22px;

  :hover {
    border: dotted #454545 1px;
  }
`;

export default IngredientBox;
